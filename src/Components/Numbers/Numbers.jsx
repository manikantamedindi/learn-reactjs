const Numbers = (props) => {
  const { name, age, place } = props.student;
  return (
    <>
      <h1>5000</h1>
      <h2>{name}</h2>
      <h2>{age}</h2>
      <h2>{place}</h2>
    </>
  );
};

export default Numbers;
